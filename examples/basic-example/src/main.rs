use ferrox::{agent::text_agent::TextAgent, Ferrox};

//In this example, the bot will be connected to a single agent.
//We also register some basic onchain functions for the agent to use.

#[tokio::main]
async fn main() {}
